<UserControl x:Class="HIO.Setup.TSetupPage2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HIO.Setup"
             mc:Ignorable="d" 
             d:DesignHeight="640" d:DesignWidth="1024">
    <StackPanel VerticalAlignment="Center"
                HorizontalAlignment="Center">

        <Image Name="Image"
               
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Stretch="Uniform"
           
               RenderOptions.BitmapScalingMode="HighQuality"  
               />
        <!--<TextBlock Text="Welcome to the HIO desktop app"
                   Foreground="{DynamicResource HIOBrightFont}"
                   Grid.Row="2"
                   Grid.Column="1"
                   VerticalAlignment="Top" 
                   HorizontalAlignment="Center" 
                   UseLayoutRounding="True"
                   Margin="0,30,0,0" 
                   FontSize="24"/>-->
    </StackPanel>
</UserControl>
